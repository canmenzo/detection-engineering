# Rules intentionally exempt from EVTX unit testing — validated by `sigma check`
# + SPL/KQL conversion in CI instead. List one rule stem per line with a reason.
# This keeps the test gate honest: an exemption must be declared, not implied.

# No public Sysmon EID 1 sample with `powershell -enc` exists; covered by the
# 4104 ScriptBlock rule (posh_ps_susp_encoded_powershell_scriptblock) which IS tested.
proc_creation_win_encoded_powershell
