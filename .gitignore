# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# Tooling
hayabusa*/
*.log

# Fetched test samples (pinned in sample_sources.yml, not vendored — see ADR 0002)
tests/.sample_cache/

# Local conversion output
out/
*.kql.txt
*.spl.txt

# OS
Thumbs.db
.DS_Store

# Keep fixture dirs but never commit captures from real machines accidentally:
# fixtures are added explicitly per-rule. Review before committing EVTX.
