# Pinned during the build — CONFIRM CURRENT VERSIONS with `pip index versions <pkg>`
# before pinning hard. The Microsoft backend has been renamed historically
# (was microsoft365defender) and is now the Kusto backend; verify at install.

pysigma>=0.11,<0.12
sigma-cli>=1.0,<2.0
pysigma-backend-splunk>=1.1,<2.0
pysigma-backend-kusto>=0.4            # Sentinel / Microsoft 365 Defender (KQL)
pyyaml>=6.0
pytest>=8.0
yamllint>=1.35
